function add() {
    let c = 1;
    let d = 2;
    console.log(c+d);
};
add();
// function helps to separate tasks and reuse codes.

function add(a,b)/*parameter */ {
    console.log(a+b);
    return true; // javascript is dynamic type language, so no need to put specific value 
};
add(1,2); //argument value // the same function is now dynamic

//function call, and argument pass








